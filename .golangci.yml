linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gofmt
    - misspell
    - gosimple
    - unused

linters-settings:
  errcheck:
    check-type-assertions: true

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
