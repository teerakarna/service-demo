version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - misspell
    - unused
  settings:
    errcheck:
      check-type-assertions: true
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
